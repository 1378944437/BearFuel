# 更新日志

BearFuel 遵循语义化版本。每个已发布的 Android APK 递增 build 号。

## [0.2.36] - 2026-08-30

### 变更

- 放弃 0.2.22–0.2.35 实验版本线，已从 GitHub 移除对应的 Release 与标签；代码基线回到 0.2.21 并重新升版发布。
- 升版至 `0.2.36+47`。

## [0.2.21] - 2026-08-30

### 修复

- 应用内检查更新不再只依赖 GitHub REST API：公开仓库 API 遇到匿名额度 403/429 时自动回退到公开 Release Atom 订阅源，再回退到公开 raw 版本清单；不会在应用内嵌入 GitHub Token。
- 检查更新按限流头、权限拒绝、暂无公开 Release、服务端错误、超时和无效数据分别提示，避免将所有失败混为“接口请求受限”。
- 实时油价 AI 审查使用专用提示词和响应解析，兼容完整 JSON、简化 JSON、代码围栏与普通文本；缺少官方金额时展示“证据不足”，不会丢弃结果或猜测价格。
- 油价 AI 审查结果不写回官方油价或账本数据。

### 变更

- 新增更新检查与油价 AI 响应解析回归测试，覆盖 API 403 限流降级、无授权请求、Atom/raw 备用源、404/5xx 与多种 AI 响应格式。
- 升版至 `0.2.21+32`。

## [0.2.20] - 2026-08-29

### 新增

- 记油表单参照小熊油耗流程优化：机显单价与加油量同排显示，机显金额独占下一行；补充优惠金额、油量警告灯与加油状态说明。
- AI 服务支持多个供应商和多种兼容接口（OpenAI、Anthropic、Gemini），每个供应商可保存多个模型，并在实际审查时选择本次使用的模型。
- 实时油价页增加 AI 油价审查与缺失调价分析入口；AI 只提供核查说明，不会用推测值覆盖官方数据。

### 修复

- “上次加油记录了吗”选择“没记录”时，统计累计行驶里程不再丢失该区间；里程统一按相邻记录差值统计，避免漏记区间丢失和未加满记录重复累计。
- 油价审查按接口价格生效日期比较，生效日期前的账单不再被错误标记为价格异常。
- 审查证据不再直接显示原始 JSON，改为中文字段和值的结构化展示。
- 记油页底部安全区与分段选择布局优化，避免窄屏内容被遮挡或溢出；说明提示图标统一为轻量信息样式。

### 变更

- 升版至 `0.2.20+31`。

## [0.2.19] - 2026-08-29

### 修复

- 记一笔加油页面底部内容被系统手势条遮挡的问题（列表底部叠加安全区）。
- "是否加满 / 油量警告灯 / 上次加油记录了吗"等分段选择题在窄屏溢出的问题（改为标签在上、选项整行等宽铺开）。

### 新增

- AI 审查支持保存多个模型：设置页可获取（多选）、手动添加、删除并指定默认模型；发起 AI 解释时可从已保存模型中选择本次使用哪一个。
- 优惠金额、实付金额与油量警告灯全界面统一展示：账本卡片显示"已优惠"与油灯警示图标，账单详情新增机显金额/优惠金额/油量警告灯，仪表盘流水标注优惠。
- 升版至 `0.2.19+30`。

## [0.2.18] - 2026-08-29

### 新增

- AI 账本审查设置支持一键获取服务商模型列表（OpenAI 兼容 `GET /models`），弹出可搜索的模型选择面板；未提供列表接口的服务会提示手动填写模型名称。
- 升版至 `0.2.18+29`。

## [0.2.17] - 2026-08-29

### 新增

- **AI 账本审查（第一版）**：本地规则引擎检查金额与量价匹配、里程回退/重复/跳跃、重复记录、超油箱容量、单价与接口价差、油耗偏离个人中位数与未来日期；新增"账本异常审查"页（筛选、AI 解释、采纳建议需人工确认、忽略与备注）；仪表盘新增待确认入口，账单详情提示待确认问题；设置 > 服务新增"AI 账本审查设置"（OpenAI 兼容接口，API Key 存于本机安全存储，支持测试连接）。未配置 AI 时本地规则审查仍然可用；AI 不直接修改账单，任何修改均需用户确认。
- **记油表单对标小熊油耗官方流程重构**：黄色"加满"提示横幅、机显单价 × 加油量 = 机显金额 三栏联动、优惠金额与实付金额区、油量警告灯记录、字段"?"说明气泡与"上次加油记录了吗"引导。
- 加油记录新增"优惠金额 / 油量警告灯"字段（数据库 v4 迁移自动升级，CSV 导入导出与备份校验同步支持）。

### 变更

- 升版至 `0.2.17+28`。

## [0.2.16] - 2026-08-29

### 修复

- 备份恢复不再拒绝含负温度的真实天气数据（此前北方用户冬季备份会被整体拒绝）。
- 加油记账取消时间选择时保留原有时间，不再被静默重置为 00:00。
- 实时油价：调价窗口日期已过时改按"等待官方公布实际调价"呈现，并自动补拉一次最新调价日历；预测/日历缓存时间戳分离，失败请求不再占用 30 分钟节流标记。

### 变更

- 图表坐标轴升级：边缘标签自动内收不再出界；纵轴按单位保留精度（金额 2 位、油耗 1 位）；小量程图表（每公里花费、油价）纵轴窗口收窄；网格与刻度对齐；纵轴宽度按实际标签测量；油价历史图相邻日期标签不再重叠。
- 万公里卡片与饼图图例防溢出；占比 <5% 的切片不再绘制互相压盖的标题。
- 数据标注：油价与调价预测展示缓存年龄；天气卡显示数据时间、超过 1 小时标注"历史快照"；统计页所选范围无完成周期时明确标注"全历史平均"；首次启动示例车辆更名为"示例车辆（可编辑）"且不再预置虚构车牌；无车辆档案时加满一箱计算显示引导而非按 50L 假定；样本无波动时不再输出异常诊断；城市定位命中缓存时提示。
- 账本回顶/搜索悬浮按钮仅在其他费用页签下隐藏；统计页切换预设范围时清空自定义日期标签。
- 升版至 `0.2.16+27`。

## [0.2.15] - 2026-08-29

### 新增

- 账本支持左右滑动切换"加油记录 / 其他费用"页签；卡片自身的左滑手势优先级更高，互不干扰。
- 其他费用页滚动现在同样会触发底部导航栏的隐藏与显现（内容不满一屏时拖动亦可触发）。

### 修复

- 底部导航栏未隐藏时，账本右下角的搜索/回顶悬浮按钮会自动上移避让，隐藏后贴边。

### 变更

- 筛选块（选站筛选、账本筛选等 8 处）选中后不再显示 ✓，统一以填充色区分选中态。
- 删除确认弹窗改为品牌风格：展示记录摘要与"可撤销"提示，取消/删除按钮层级更清晰。
- 安装包体积优化：启用 R8 代码收缩与资源收缩、开启 Dart 混淆，调试符号随构建产物保存 90 天。
- 升版至 `0.2.15+26`。

## [0.2.14] - 2026-08-29

### 新增

- 全新应用内紧凑日期选择器：与品牌风格统一（周一首列、选中日品牌橙圆底、今日描边），替换系统默认日期弹窗，覆盖加油记账、费用记账、保养提醒与统计自定义范围。

### 修复

- 账本左滑后点击"编辑/删除"无效的问题：手势命中框未跟随卡片平移，导致滑开后按钮点击被卡片吞掉。
- 账本左滑动画中卡片圆角与操作抽屉衔接处边角不协调的问题。
- 统计与油价图表的触摸气泡在触点靠近边界时超出可视范围的问题。

### 变更

- 发布流程改为远端优先：删除本地发布脚本，推送 vX.Y.Z 标签即触发 GitHub Actions 构建并发布 Release；本地构建命令保留在文档与"构建与发布"说明中。
- 移除"关于应用"中的 GitHub Release 地址入口（与"构建与发布"的打开发布页重复）；检查更新对接口限流（403）与私有仓库（404）给出明确提示。
- 设置页列表项移除前方数字序号；更新日志（CHANGELOG.md）全部改为中文并随包内置。

## [0.2.13] - 2026-08-29

### 新增

- "关于应用"设置板块：当前版本与 Build 号、GitHub Release 地址与检查更新、应用内更新日志、构建与发布入口。
- 一键发布流水线 `scripts/release.sh`：一条命令完成升版（patch/minor/major）、本地构建（analyze + test + 分架构 Android APK，macOS 附带未签名 iOS IPA）与远端构建（提交、打 tag 并推送，触发 GitHub Actions 发布）。
- 更新日志（CHANGELOG.md）随安装包打包，应用内可直接浏览历届版本变更。

### 变更

- README 重写：补充构建与发布流水线、产物矩阵（签名 APK×3 + 未签名 IPA）与签名配置说明。
- Android Kotlin 插件升级至 2.2.0，适配 `package_info_plus` 及其依赖的 Kotlin 2.2 元数据。
- 升版至 `0.2.13+24`。

## [0.2.12] - 2026-08-29

### 修复

- CSV 导出→导入往返不再损坏"总价"：表头"每公里花费"改挂独立派生列，不再抢占总价列（列匹配改为首匹配生效）。
- 部分加油（未加满）记录不再重复计入里程：周期统计、万公里统计、365 天热力图与总览里程均只累计完成测量周期的里程。
- 追加模式导入 CSV 自动按"同车 + 同一分钟 + 同里程"去重，并提示"导入 N 条、跳过 M 条重复"，不再把历史翻倍。
- 账本"自定义"时间范围真正生效；"全部时间"不再隐藏补录的未来日期记录；统计自定义范围不再漏进起始日的前一天数据。
- 加油/费用记账保存按钮增加防抖与加载态，杜绝连点产生重复记录。
- 覆盖导入前增加二次确认（提示将被清空的记录数）。
- 行程统计按完整日期分组，不再把不同年份的同月同日合并。
- 账本左滑卡片可再次退出：点击卡片、右滑、滚动或滑开其他卡片均可收起。
- 图表坐标轴：趋势图/温度图/油价图统一采用自适应刻度与抽稀标签，修复横纵坐标拥挤与标签重叠。
- 城市搜索覆盖全国约 340 个地级市（支持中文/省份/全拼/首字母）；城市面板不再自动弹 GPS 权限，键盘弹出不再遮挡内容。
- 定位、高德、ApiZero 油价/预测、墨迹天气等网络响应体读取全部加超时，平台逆地理编码限时 6 秒，杜绝"正在定位"永久悬挂。
- 加油站搜索请求结束后会用最新定位补查；仪表盘跑马灯不再叠加双循环；日期范围弹窗补 mounted 守卫；车牌控件响应 initialPlate 变化。
- 删除最后一辆车后清空残留的记录状态；车辆弹窗关闭时释放输入控制器。

### 变更

- Release 包不再向系统日志输出 GPS 坐标与数据库路径（日志跟随 kDebugMode）。
- 车辆写入改用 abort 冲突策略，消除级联清空陷阱；全量备份导出改为单事务快照；数据库懒初始化 Future 记忆化。
- 三大组件硬编码品牌色统一到语义 token；修正 AppRadius 撞值；车牌控件补齐无障碍标签。
- Android targetSdk 升至 35 并恢复 release lint；pubspec Dart 下限对齐实际、flutter_lints 升至 6；release 工作流动态解析 build-tools；CI 增加 iOS 编译验证。
- 升版至 `0.2.12+23`。

## [0.2.11] - 2026-08-28

- 完成剩余审计优化与后续修复。
- 统一 Dart 格式化并修复分析器告警。

## [0.2.10] - 2026-08-28

- 修复空备份校验、城市到省份映射、CSV 往返与车辆切换失败处理。
- 新增 iOS 外链处理，改进天气图表数据范围。
- 规范 CI 格式检查与分析器配置。

## [0.2.9] - 2026-08-28

- 恢复永久发布签名与分架构 APK 签名校验。
- 移除已废弃的报告分享、PDF 导出与无用 UI 组件。
- 重写 GitHub 项目文档并清理过时本地产物。

## [0.2.8] - 2026-08-28

- 修复最后已知地图位置使用硬编码荆门坐标的问题。
- 修复记录时间范围边界、天气快照回退与调价预测冷启动限频。
- 修复备份版本元数据过期、计算回写失败被吞与缓存油价未标注。
- 发布自动化改为输出未签名分架构 Android APK 与未签名 iOS IPA。

## [0.2.7] - 2026-08-28

- 发布 `armeabi-v7a`、`arm64-v8a`、`x86_64` 独立 APK。
- 设置拆分为服务与备份两个板块。
- Android 版本升至 `0.2.7+18`。

## [0.2.6] - 2026-08-28

- 发布自动化改为仅输出 Android 全架构 APK 与 iOS 未签名 IPA。
- 移除首次运行的应用标识迁移提示。
- Android 版本升至 `0.2.6+17`。

## [0.2.5] - 2026-08-28

- 以共享的明暗设计系统重建应用界面。
- 全部可见图标迁移至 Lucide 并移除表情符号。
- 优化仪表盘、导航、设置、油价与选站界面。
- Android 发布目标默认保留 `arm64-v8a`。
- 新增永久自管 Android 发布签名配置。
- 新增 GitHub CI 与标签驱动的发布自动化。
